import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.demensdeum.teflecher.editor',
  appName: 'Teflecher Editor',
  webDir: 'dist'
};

export default config;
